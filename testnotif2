<button onclick="Send('Clicked','You have clicked the button')">Click me</button>
<script>
  function AskNotification(){
    if (Notification.permission === "granted"){
      console.log("We have permission to send notifications");
      //Send("PLANNER", "Hello!");
    } else if (Notification.permission === "default"){
      Notification.requestPermission().then(permission => {console.log("permission: ",permission);});
    }
  }
        
  function Send(title, text){
    var img = "./favicon.png";
    var notification = new Notification(title, {body: text, icon: img});
    console.log("Notification sent");
  }

  AskNotification()
  Send("Welcome!","This is a test!")
</script>
